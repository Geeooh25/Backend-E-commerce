# Beedaht Sweet Treats Backend API

## Installation

1. Clone the repository
2. Run `npm install`
3. Copy `.env.example` to `.env` and update the values
4. Run `npm run dev` for development
5. Run `npm start` for production

## API Documentation

### Base URL